//import { ADD_COMPANY, GET_COMPANY } from '../constants/ActionTypes'
const initialState = {
}
const customerReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default customerReducer;